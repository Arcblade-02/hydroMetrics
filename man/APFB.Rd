% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/APFB.R
\name{APFB}
\alias{APFB}
\title{Evaluate annual peak flow bias}
\usage{
APFB(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar with class \code{"hydro_metric_scalar"}.
}
\description{
\code{APFB()} is an indexed compatibility wrapper over \code{\link[=gof]{gof()}} for the registry
metric \code{"apfb"}. It requires univariate \code{zoo} or \code{xts} inputs with a time
index so yearly maxima can be derived deterministically.
}
\examples{
if (requireNamespace("zoo", quietly = TRUE)) {
  dates <- as.Date(c("2020-01-01", "2020-06-01", "2021-01-01", "2021-06-01"))
  sim <- zoo::zoo(c(12, 18, 33, 35), order.by = dates)
  obs <- zoo::zoo(c(10, 20, 30, 40), order.by = dates)
  APFB(sim, obs)
}
}
