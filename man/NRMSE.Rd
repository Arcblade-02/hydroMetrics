% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phase2_compat_wrappers.R
\name{NRMSE}
\alias{NRMSE}
\title{Evaluate the legacy normalized RMSE wrapper}
\usage{
NRMSE(sim, obs, norm = c("mean"), na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{norm}{Normalization method. Phase 2 supports \code{"mean"} only.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Thin exported wrapper over \code{\link[=gof]{gof()}} for the registry metric \code{"nrmse"}.
Phase 2 freezes the public \code{norm} contract at \code{norm = "mean"} to represent
CV-RMSE style normalization by \code{mean(obs)}.
}
\examples{
NRMSE(c(1, 2, 3), c(1, 2, 2), norm = "mean")
}
