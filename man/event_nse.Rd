% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_layer_b.R
\name{event_nse}
\alias{event_nse}
\title{Evaluate the event NSE wrapper}
\usage{
event_nse(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Thin exported wrapper over \code{\link[=gof]{gof()}} for the registry metric \code{"event_nse"}.
Observed event windows are defined as contiguous runs of observed values
strictly above the observed 0.8 quantile and the score is computed as NSE on
the pooled observed event windows.
}
\examples{
obs <- c(1, 2, 5, 6, 2, 1, 1, 4, 5, 2, 1, 1)
sim <- c(1, 2, 4, 7, 2, 1, 1, 3, 6, 2, 1, 1)
event_nse(sim, obs)
}
