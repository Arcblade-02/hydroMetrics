% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HFB.R
\name{high_flow_percent_bias}
\alias{high_flow_percent_bias}
\title{Evaluate the high-flow percent bias wrapper}
\usage{
high_flow_percent_bias(sim, obs, threshold_prob = 0.9, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{threshold_prob}{Probability threshold used to define the high-flow
subset from \code{obs}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar with class \code{"hydro_metric_scalar"}.
}
\description{
\code{high_flow_percent_bias()} is the canonical exported wrapper over \code{\link[=gof]{gof()}}
for the registry metric \code{"high_flow_percent_bias"}. The exact failure mode
for sparse high-flow support is preserved as part of the public wrapper
contract. Stable condition contract: it errors on invalid \code{threshold_prob},
on downstream preprocessing failures, and when fewer than three high-flow
points remain; it warns and returns \code{NA} when the high-flow denominator is
zero.
}
\examples{
high_flow_percent_bias(1:30 + 1, 1:30)
}
