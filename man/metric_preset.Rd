% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_preset.R
\name{metric_preset}
\alias{metric_preset}
\title{Resolve a documented metric preset to canonical metric ids}
\usage{
metric_preset(preset, exported_only = FALSE)
}
\arguments{
\item{preset}{One or more documented preset names.}

\item{exported_only}{Logical; if \code{TRUE}, keep only metric ids that also have
an exported wrapper path in the current package surface.}
}
\value{
A character vector of canonical metric ids suitable for
\code{gof(methods = ...)}.
}
\description{
\code{metric_preset()} provides a small bridge between discovery and execution.
It resolves one or more documented preset-group names to the canonical
metric ids accepted by \code{\link[=gof]{gof()}} \code{methods = ...}.
}
\details{
The helper is intentionally modest for the current Workstream C scope. It
returns a plain character vector, does not create a new S3 class, and does
not wrap or bypass \code{\link[=gof]{gof()}}.

Available preset groups:
\itemize{
\item \code{recommended}
\item \code{compatibility_core}
\item \code{deterministic_error}
\item \code{correlation_agreement}
\item \code{flow_duration_distribution}
\item \code{probabilistic_uncertainty}
\item \code{seasonal_regime}
}
}
\examples{
ids <- metric_preset("recommended")
ids

sim <- c(1.1, 1.9, 3.2, 4.1)
obs <- c(1.0, 2.0, 3.0, 4.0)
gof(sim, obs, methods = ids)
}
