% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_search.R
\name{metric_search}
\alias{metric_search}
\title{Search registered metrics by simple discovery metadata}
\usage{
metric_search(
  text = NULL,
  category = NULL,
  tags = NULL,
  preset = NULL,
  exported = NULL,
  compatibility = NULL
)
}
\arguments{
\item{text}{Optional case-insensitive text query matched against metric id,
name, description, exported wrapper names, and preset labels.}

\item{category}{Optional category filter matched against registry
\code{category}.}

\item{tags}{Optional registry-tag filter matched against the semicolon-joined
\code{tags} field.}

\item{preset}{Optional preset-group filter. Must be one or more of the
documented preset names.}

\item{exported}{Optional logical filter. \code{TRUE} keeps only metrics that have
at least one exported wrapper path; \code{FALSE} keeps only metrics that are not
directly exposed by an exported wrapper.}

\item{compatibility}{Optional logical filter. \code{TRUE} keeps only metrics that
are reached by at least one documented compatibility export.}
}
\value{
A \code{data.frame} of registry metrics with discovery annotations.
}
\description{
\code{metric_search()} provides a small discovery-oriented view over the current
metric registry. It reuses existing registry metadata and adds a few
user-facing annotations that are already supportable from the current package
surface:
}
\details{
\itemize{
\item \code{exported_wrappers}: exported wrapper names that route to the metric id
\item \code{compatibility_export}: whether a documented compatibility export reaches
that metric id
\item \code{presets}: curated baseline preset groups for Workstream C discovery
}

This first baseline is intentionally modest. It can filter by text,
category, tags, preset group, and exported/compatibility status, but it does
not search formulas, inspect applicability conditions, or replace the metric
reference vignette.

Available preset groups:
\itemize{
\item \code{recommended}
\item \code{compatibility_core}
\item \code{deterministic_error}
\item \code{correlation_agreement}
\item \code{flow_duration_distribution}
\item \code{probabilistic_uncertainty}
\item \code{seasonal_regime}
}

\code{tags} and \code{preset} filters use case-insensitive any-match logic across the
supplied values.
}
\examples{
metric_search(text = "bias")
metric_search(category = "correlation")
metric_search(tags = "probabilistic")
metric_search(preset = "compatibility_core")
}
