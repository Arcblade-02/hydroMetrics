% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_layer_c.R
\name{mutual_information_score}
\alias{mutual_information_score}
\title{Evaluate the mutual information score wrapper}
\usage{
mutual_information_score(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Compatibility export retained alongside canonical \code{\link[=mutual_information]{mutual_information()}}.
This wrapper remains callable for continuity, but it is not a separate
discovery-canonical metric name. Under the current deterministic policy it
reports the same raw pooled-grid mutual information in natural-log units as
\code{\link[=mutual_information]{mutual_information()}}. It is retained as a compatibility surface on the
current \verb{0.4.x} line rather than as an independent lifecycle lane.
}
\examples{
mutual_information_score(c(1, 2, 2, 3, 4, 5), c(1, 1, 2, 3, 3, 4))
}
