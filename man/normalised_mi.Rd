% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_layer_c.R
\name{normalised_mi}
\alias{normalised_mi}
\title{Evaluate the normalised mutual information wrapper}
\usage{
normalised_mi(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Thin exported wrapper over \code{\link[=gof]{gof()}} for the canonical registry metric
\code{"normalised_mi"}. The metric uses pooled-support Sturges histograms and
reports \code{MI / sqrt(H_sim * H_obs)} in natural-log units. Zero-entropy
normalization cases are rejected explicitly.
}
\examples{
normalised_mi(c(1, 2, 2, 3, 4, 5), c(1, 1, 2, 3, 3, 4))
}
