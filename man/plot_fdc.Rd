% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_fdc.R
\name{plot_fdc}
\alias{plot_fdc}
\title{Plot an observed vs simulated flow duration curve}
\usage{
plot_fdc(
  sim,
  obs,
  na_strategy = c("fail", "remove", "pairwise"),
  sim_label = "Simulated",
  obs_label = "Observed",
  title = NULL,
  x_lab = "Exceedance probability (\%)",
  y_lab = "Flow",
  log_scale = FALSE
)
}
\arguments{
\item{sim}{Simulated values as a supported single-series input accepted by
\code{\link[=preproc]{preproc()}}.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na_strategy}{Missing-value strategy forwarded to \code{\link[=preproc]{preproc()}}.}

\item{sim_label}{Legend label used for the simulated FDC.}

\item{obs_label}{Legend label used for the observed FDC.}

\item{title}{Optional plot title.}

\item{x_lab}{X-axis label. Defaults to \code{"Exceedance probability (\%)"}.}

\item{y_lab}{Y-axis label. Defaults to \code{"Flow"}.}

\item{log_scale}{Logical; if \code{TRUE}, use a log-scaled y-axis.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_fdc()} is a lightweight static plotting helper for comparing observed
and simulated flow duration curves. It reuses \code{\link[=preproc]{preproc()}} for supported
single-series alignment and missing-value handling, then constructs a simple
empirical exceedance-probability plot from the aligned values.
}
\details{
Exceedance probability is computed deterministically as
\code{100 * i / (n + 1)} on the descending sorted aligned values of each series.
The helper returns a \code{ggplot2} object when \code{ggplot2} is available through
\code{Suggests}, and it errors clearly when that optional dependency is
unavailable.

If \code{log_scale = TRUE}, all aligned values must be strictly positive. The
helper errors otherwise rather than silently dropping or shifting values.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  sim <- c(1.1, 1.9, 3.2, 4.1)
  obs <- c(1.0, 2.0, 3.0, 4.0)

  plot_fdc(sim, obs)
  plot_fdc(sim, obs, log_scale = TRUE)
}
}
