% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hydrograph.R
\name{plot_hydrograph}
\alias{plot_hydrograph}
\title{Plot an observed vs simulated hydrograph comparison}
\usage{
plot_hydrograph(
  sim,
  obs,
  na_strategy = c("fail", "remove", "pairwise"),
  sim_label = "Simulated",
  obs_label = "Observed",
  title = NULL,
  x_lab = "Index",
  y_lab = "Value"
)
}
\arguments{
\item{sim}{Simulated values as a supported single-series input accepted by
\code{\link[=preproc]{preproc()}}.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na_strategy}{Missing-value strategy forwarded to \code{\link[=preproc]{preproc()}}.}

\item{sim_label}{Legend label used for the simulated series.}

\item{obs_label}{Legend label used for the observed series.}

\item{title}{Optional plot title.}

\item{x_lab}{Optional x-axis label. Defaults to \code{"Index"}.}

\item{y_lab}{Y-axis label. Defaults to \code{"Value"}.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{plot_hydrograph()} is the first lightweight static plotting helper for the
package. It keeps plotting separate from \code{\link[=ggof]{ggof()}} and reuses \code{\link[=preproc]{preproc()}} for
supported single-series alignment and NA handling before constructing a
simple observed-vs-simulated line plot.
}
\details{
The helper is intentionally modest for Phase 4: it returns a \code{ggplot2}
object when \code{ggplot2} is available through \code{Suggests}, and it errors
clearly when that optional dependency is unavailable.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  sim <- c(1.1, 1.9, 3.2, 4.1)
  obs <- c(1.0, 2.0, 3.0, 4.0)

  plot_hydrograph(sim, obs)
}
}
