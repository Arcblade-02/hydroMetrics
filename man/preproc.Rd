% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preproc.R
\name{preproc}
\alias{preproc}
\title{Preprocess hydrological series}
\usage{
preproc(
  sim,
  obs,
  na_strategy = c("fail", "remove", "pairwise"),
  transform = c("none", "log", "sqrt", "reciprocal"),
  epsilon_mode = c("constant", "auto_min_positive", "obs_mean_factor"),
  epsilon = NULL,
  epsilon_factor = 1,
  na.rm = NULL,
  keep = NULL,
  epsilon.type = NULL,
  epsilon.value = NULL,
  ...
)
}
\arguments{
\item{sim}{Simulated values as a numeric vector or supported indexed
single-series object.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na_strategy}{Missing-value strategy used by the preprocessing engine.}

\item{transform}{Optional transform applied after NA handling.}

\item{epsilon_mode}{Epsilon policy used when a transform requires adjustment.}

\item{epsilon}{Constant epsilon value used when
\code{epsilon_mode = "constant"}.}

\item{epsilon_factor}{Scaling factor for automatic epsilon modes.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{keep}{Optional compatibility alias for NA handling. \code{"complete"} maps
to \code{na_strategy = "remove"} and \code{"pairwise"} maps to \code{"pairwise"}.}

\item{epsilon.type}{Optional compatibility alias for \code{epsilon_mode}.}

\item{epsilon.value}{Optional compatibility alias for the epsilon numeric
value. It maps to \code{epsilon} when \code{epsilon_mode = "constant"} and to
\code{epsilon_factor} otherwise.}

\item{...}{Additional compatibility arguments retained for forward
compatibility.}
}
\value{
A list with class \code{"hydro_preproc"} containing processed vectors and
metadata, including \code{sim}, \code{obs}, \code{n_original}, \code{n_aligned},
\code{n_removed_na}, \code{transform_applied}, and \code{epsilon_details}. The documented
return fields and error behavior are part of the stable public contract.
}
\description{
Stable orchestration entry point for input preprocessing. The current
exported contract supports single-series numeric, \code{ts}, \code{zoo}, and \code{xts}
inputs and intentionally rejects matrix/data.frame inputs.
Stable condition contract: \code{preproc()} errors on unsupported public input
types, unequal lengths after required non-indexed validation, invalid
\code{na_strategy}/compatibility alias values, conflicting epsilon aliases, and
missing values when \code{na_strategy = "fail"}. Indexed \code{zoo}/\code{xts} inputs are
deterministically aligned on their common index before NA handling.
}
\examples{
preproc(c(1, NA, 3), c(1, 2, 3), na.rm = TRUE)
}
