% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_layer_b.R
\name{seasonal_nse}
\alias{seasonal_nse}
\title{Evaluate the seasonal NSE wrapper}
\usage{
seasonal_nse(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Thin exported wrapper over \code{\link[=gof]{gof()}} for the registry metric \code{"seasonal_nse"}.
This metric requires monthly seasonality that can be inferred from a monthly
\code{ts} series or from aligned date-like indexed input.
}
\examples{
sim <- ts(rep(c(10, 12, 9, 8, 7, 6, 5, 6, 7, 8, 9, 11), 2), frequency = 12)
obs <- ts(rep(c(9, 11, 10, 8, 6, 6, 5, 5, 8, 8, 10, 10), 2), frequency = 12)
seasonal_nse(sim, obs)
}
