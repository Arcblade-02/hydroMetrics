% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers_layer_c.R
\name{tail_dependence_score}
\alias{tail_dependence_score}
\title{Evaluate the tail dependence score wrapper}
\usage{
tail_dependence_score(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs.
}
\description{
Deprecated forwarding wrapper for \code{\link[=upper_tail_conditional_exceedance]{upper_tail_conditional_exceedance()}}.
}
\details{
\code{tail_dependence_score()} remains exported temporarily for compatibility,
but \code{"tail_dependence_score"} is no longer a live canonical registry metric
id. Calls emit a deprecation warning and then forward directly to canonical
\code{\link[=upper_tail_conditional_exceedance]{upper_tail_conditional_exceedance()}}.
}
\examples{
tail_dependence_score(c(1, 2, 3, 7, 8, 4), c(1, 2, 4, 8, 7, 5))
}
