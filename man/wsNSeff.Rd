% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wsNSeff.R
\name{wsNSeff}
\alias{wsNSeff}
\title{Evaluate the legacy weighted-squared NSE wrapper}
\usage{
wsNSeff(sim, obs, na.rm = NULL, ...)
}
\arguments{
\item{sim}{Simulated values supplied as a numeric vector, \code{ts}, matrix-like
object, or aligned \code{zoo}/\code{xts} series.}

\item{obs}{Observed values with the same shape contract as \code{sim}.}

\item{na.rm}{Optional compatibility alias for NA handling. \code{TRUE} maps to
\code{na_strategy = "remove"} and \code{FALSE} maps to \code{"fail"}.}

\item{...}{Additional compatibility arguments, including \code{metric_params}.}
}
\value{
A numeric scalar for single-series inputs or a numeric vector for
multi-series inputs. The documented return shape and failure behavior are
preserved as part of the compatibility contract.
}
\description{
Compatibility export retained for legacy hydroGOF-style wrapper continuity.
This thin wrapper delegates to \code{\link[=gof]{gof()}} for the registry metric \code{"wsnse"}.
It inherits NA handling, input-shape checks, and the canonical \code{"wsnse"}
domain and denominator guards from \code{\link[=gof]{gof()}}.
}
\examples{
wsNSeff(c(1, 2, 3), c(1, 2, 2))
}
