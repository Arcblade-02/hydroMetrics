Phase 2 Compatibility Audit - ggof() Runtime Contract
Evidence class: verified fact
accepted vector input = TRUE
accepted data.frame input = TRUE
accepted zoo input = TRUE
return class = hydro_metrics_batch|data.frame
noninteractive device list unchanged = TRUE
include_meta adds columns = TRUE
deterministic repeated run = TRUE
mismatched lengths error = `sim` and `obs` must have the same length.

Case details:
- vector_single_metric: none
- vector_include_meta: none
- data_frame_single_metric: none
- zoo_single_metric: none
- mismatch_error: error:`sim` and `obs` must have the same length.

Evidence class: likely inference
ggof() constructs a deterministic hydro_metrics_batch data.frame in noninteractive use and does not open a graphics device in these local checks.

Evidence class: recommendation
Treat the observed return class as a compatibility-relevant plotting divergence candidate if downstream code expects a plot object.
