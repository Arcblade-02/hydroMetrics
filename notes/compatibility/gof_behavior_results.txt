Phase 2 Compatibility Audit - gof() Runtime Contract
Evidence class: verified fact
accepted vector input = TRUE
accepted data.frame input = TRUE
accepted zoo input = TRUE
single-metric return class = hydro_metrics
multi-metric return class = hydro_metrics
multi-metric metric names = NSE, pbias
fun alias accepted = TRUE
deterministic repeated run = TRUE
mismatched lengths error = `sim` and `obs` must have the same length.

Case details:
- vector_single_metric: none
- vector_multi_metric: none
- data_frame_single_metric: none
- zoo_single_metric: none
- fun_alias: none
- mismatch_error: error:`sim` and `obs` must have the same length.

Evidence class: likely inference
gof() accepts several public input shapes and returns a stable hydro_metrics object on repeated representative inputs.

Evidence class: recommendation
Use the mismatch and alias evidence here when assessing wrapper-compatibility fixes around the gof() entry point.
